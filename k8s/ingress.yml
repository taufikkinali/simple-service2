apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pgmicroservice-ingress
  namespace: taufikkinali          # change if your namespace is different
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - host: taufikkinali.57.158.184.247.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pgmicroservice-svc
                port:
                  number: 80
